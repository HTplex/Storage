DATA SEGMENT
	BUFFER DB 10 DUP(?) 
DATA ENDS

CODE SEGMENT
	ASSUME CS:CODE, DS:DATA
	START: 

	MOV AX, SEG BUFFER
	MOV DS, AX
	MOV DX, OFFSET BUFFER
	MOV AH, 10
	INT 21H

	MOV AX, SEG BUFFER
	MOV DS, AX
	LEA DX, BUFFER
	MOV AH, 9
	INT 21H
	MOV AH, 4CH
	INT 21H
CODE ENDS
	END START 
